#!/data/data/com.termux/files/usr/bin/bash

echo "🔄 Updating system and installing dependencies..."
# Update packages and install Node.js, SQLite, and Curl
pkg install nodejs sqlite curl -y

echo "📦 Installing npm packages..."
# Install required npm modules
npm install axios cli-table3

echo "📥 Downloading script..."
# Download the file from your repo
# Note: I cleaned up the URL to the standard GitHub raw format
curl -sL "https://raw.githubusercontent.com/abinoldi/muffzle/main/randservkai" -o randservkai.js

echo "🚀 Running script..."
# Run the script using Node.js
node randservkai.js
